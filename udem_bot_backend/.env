DB_HOST=localhost          // Dónde está PostgreSQL (tu propia computadora).
DB_PORT=5432               // Puerto estándar de PostgreSQL.
DB_NAME=udem_bot_db        // Nombre de tu base de datos.
DB_USER=postgres           // Usuario que creaste (o "postgres" por defecto).
DB_PASSWORD=Bloom1202*  // La contraseña que pusiste.
PORT=3000                  // Puerto para el servidor.